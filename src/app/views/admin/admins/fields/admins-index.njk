<section class="fields-creation">
    <div class="div-fields-creation">
        <div>
            <p class="title">Nome</p>
            <input type="text" name="name" value="{{ chef.name }}" placeholder="Insira seu nome" required>
        </div>
        <div>
            <p class="title">Email</p>
            <input type="text" name="email" value="{{ chef.name }}" disabled placeholder="Digite seu email" required>
        </div>
        <div>
            <p class="title">Senha</p>
            <input type="text" name="password" value="{{ chef.name }}" placeholder="Digite sua senha" required>
        </div>
    </div>
</section>
