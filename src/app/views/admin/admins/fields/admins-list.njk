<section class="fields-creation">
    <div class="div-fields-list">
        {% for user in users %}
            <div class="list">
                <p class="name">{{ user.name }}</p>
                <p class="email">{{ user.email }}</p>
                <div class="list-links">
                    <form id="form-delete" class="form-delete list-form-delete" action="/admin/admins?_method=DELETE" method="POST">
                        <input type="hidden" name="id" value="{{ user.id }}">
                        <a class="list-links-a" href="/admin/admins/{{ user.id }}/edit">Editar</a>
                        {% if user.id != session.userId %}
                            <button type="submit" id="button-delete" class="list-links-b">Remover</button>
                        {% endif %}
                    </form>
                </div>
            </div>
        {% endfor %}
    </div>
</section>